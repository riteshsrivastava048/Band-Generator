<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles/output.css">
    <link rel="stylesheet" href="styles/input.css">
</head>
<body>
    <main class="bg-black min-h-screen">
        <section class="text-pink-700 w-full flex flex-col justify-center items-center" style="height: calc(100vh - 2rem);">
            <div class="font-roboto font-extra-bold my-5 text-[35px]">
              <% if(locals.nameSend){ %>
                <%=nameSend%>
              <%} else { %>
                Welcome To Random Name Generator
              <% } %>
            </div>
            <a href="/submit"><button class="hover:bg-white hover:text-black hover:pointer-cursor rounded-sm py-1 px-3 border-white text-white bg-slate-600">Generate</button></a>
        </section>
        <footer class="text-[10px] fixed bottom-0 pb-0 w-full text-pink-700">Copyright@ <%= td%></footer>
    </main>
</body>
</html>